---
- name: Ensure packages are installed
  become: true
  ansible.builtin.package:
    name:
      - acpi
      - bmon
      - htop
      - iftop
      - lm-sensors
      - powertop
      - smartmontools
      - tcpdump
      - upower
      - wireguard-tools
    state: present
