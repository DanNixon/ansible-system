---
- name: Ensure packages are installed
  become: true
  package:
    name:
      - acpi
      - bmon
      - hddtemp
      - htop
      - iftop
      - lm-sensors
      - powertop
      - smartmontools
      - upower
    state: present
