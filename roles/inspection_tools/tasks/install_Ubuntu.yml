---
- name: Ensure packages are installed
  become: true
  ansible.builtin.package:
    name:
      - acpi
      - bmon
      - hddtemp
      - htop
      - iftop
      - lm-sensors
      - powertop
      - smartmontools
      - tcpdump
      - upower
      - wireguard-tools
    state: present
